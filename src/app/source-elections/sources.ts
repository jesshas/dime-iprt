/*this will be for live data*/
export class Sources {
    constructor (
        public sourceName: string,
        public sourceValue: string)
        {
            this.sourceName = sourceName;
            this.sourceValue = sourceValue;
        }
}